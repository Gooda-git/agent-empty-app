{
  "name": "gooda-project-cockpit",
  "version": "0.1.0",
  "private": true,
  "description": "First minimal runnable product scaffold for the Gooda agent harness.",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 4173 --directory src",
    "validate": "node --check src/app.js && node tests/static-validate.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
